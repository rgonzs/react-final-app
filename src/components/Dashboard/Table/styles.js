import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    title: {
      marginTop: 6,
    },
    table: {
      minWidth: 100,
    },
    paper: {
      height: 250,
      borderRadius: '25px',
    },
  });

export default useStyles